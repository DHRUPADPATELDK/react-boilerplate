import React from 'react';

import FooterStyles from './styles.scss';

const FooterComponent: any = () => {
  return <footer className={FooterStyles.footer}>footer</footer>;
};

export default FooterComponent;
